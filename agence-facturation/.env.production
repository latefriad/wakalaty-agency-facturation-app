# Build de production (npm run build) — servi sous https://wakalati.app
# L'API est sur le même domaine, exposée sous /api via le reverse proxy Plesk.
REACT_APP_API_URL=/api
DISABLE_ESLINT_PLUGIN=true
ESLINT_NO_DEV_ERRORS=true
CI=false
